import service from './index'

export default{
    // 1. 获取定位(get)
    position(){
        return service.get(`/position`)
    },
     // 2. 获取定位(get)
     position(){
        return service.get(`/position`)
    },
     // 3. 获取定位(get)
     position(){
        return service.get(`/position`)
    },
}